<?xml version="1.0"?>
<project name="Rectangle-Test" default="main" basedir=".">
	<property environment="env"/>

	<property name="src.dir" location="tests/src" />
	<property name="build.dir" location="tests/bin" />
	<property name="report.dir" location="tests/reports" />
	<property name="lib.dir" location="tests/lib" />
	<property name="code.src.dir" location="src" />

	<path id="class.path">
		<fileset dir="${lib.dir}" includes="**/*.jar" />
		<pathelement location="${build.dir}" />
	</path>

	<target name="clean">
		<delete dir="${build.dir}"  />
		<delete dir="${report.dir}" />
	</target>

	<target name="makedir" depends="clean">
		<mkdir dir="${build.dir}" />
		<mkdir dir="${report.dir}" />
	</target>

	<target name="compile" depends="makedir">
		<javac destdir="${build.dir}">
			<src path="${src.dir}" />
			<src path="${code.src.dir}" />
			<classpath refid="class.path" />
		</javac>
	</target>

	<target name="test" depends="clean, makedir, compile">
		<junit printsummary="yes" fork="true">
			<classpath refid="class.path" />
			<test name="RectangleTest" todir="${report.dir}" outfile="result">
				<formatter type="xml" />
			</test>
		</junit>
	</target>

	<target name="main" depends="test">
		<description>Main target</description>
	</target>
</project>
